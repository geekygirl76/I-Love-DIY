<head>
 <style>
   nav.new-message{
     width: 700px;
     height: auto;
     margin:auto;
     background: rgb(228, 234, 233);
   padding: 5px 10px;
   border: 1px solid lightblue;
   }

   form.message-form{
     width:700px;
     min-height:500px;
     background: rgb(241, 246, 245);
     margin:auto;
   padding: 5px 10px;
   border: 1px solid lightblue;
   }


 </style>
</head>


<body>

  <nav class='new-message'>
      <h1><a href="<%= messages_url %>">Inbox</a></h1>
   </nav>
<form class="message-form" action="<%= messages_url %>" method="post" >
  <input type="hidden" name="authenticity_token"
         value="<%= form_authenticity_token %>" >




    <label for="message_receiver_id">Sent To:</label>
    <select name="message[receiver_id]" id="message_receiver_id">
      <option value="">Send To</option>
      <% User.order(:username).each do |user| %>
      <% if user.username!= "" && user.id != current_user.id %>
        <option
          <%= @message.receiver_id == user.id ? "selected" : nil  %>
          value="<%= user.id %>"
          >

          <%= user.username %>
        </option>
    <% end %>
      <% end %>
    </select>
    <br>



   <label for="message_title">
     Title
   <input
     type="text"
     name="message[title]"
     value="<%= @message.title %>"
     id="message_title">
   </label>
     <br>




   <label for="message_body" >
     Letter
   <textarea class="article"

     name="message[body]"
     value="<%= @message.title %>"
     id="message_body"><%= @message.body %>
     </textarea>
   </label>
     <br>


   <label for="message_draft">
     Is Draft?(Y or N)
   <input
     type="text"
     name="message[draft]"

     id="message_draft">
   </label>
     <br>

    <button class="create-post">

     <input type="submit" value="Submit">
     </button>

</form>

<footer>
  <li><h4>Do It Yourself, Make It Unique!</h4></li>
</footer>
</body>