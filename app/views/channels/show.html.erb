<h1>Category: <%= @channel.sub.title %></h1>

<h2>Channel: <%= @channel.title %></h2>
<br>
<ul>
  <h3>Instructables</h3>
  <% @channel.posts.each do |post| %>
   <li><%= link_to post.title, post_url(post) %>
     <%= image_tag post.photo.url(:thumb) %>
   </li>
   <br>
 <% end %>

</ul>